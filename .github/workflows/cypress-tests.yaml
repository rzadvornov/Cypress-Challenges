name: Cypress Parallel Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  cypress-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2, 3] # Adjust this number to match your desired parallelization level
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build Docker image
      run: docker build --no-cache -t cypress-split-image .

    - name: Run Cypress tests in parallel
      run: |
        docker run \
          -e SPLIT_TOTAL_MACHINES=${{ strategy.job-total }} \
          -e SPLIT_MACHINE_INDEX=${{ strategy.job-index }} \
          cypress-split-image

    - name: Upload test results
      uses: actions/upload-artifact@v4
      with:
        name: cypress-results
        path: |
          cypress/reports
          cypress/screenshots
          cypress/videos
        if-no-files-found: ignore

  publish-results:
    needs: cypress-tests
    runs-on: ubuntu-latest
    if: always()
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download test results
      uses: actions/download-artifact@v3
      with:
        name: cypress-results
        path: ./cypress-results

    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./cypress-results/cypress/reports
        keep_files: true